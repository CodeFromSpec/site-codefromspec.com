# SPEC/design/404

Error page. Centered layout with the 404 image as
the visual focus, a short heading below it, and a
link back to the home page.

# Public

## Theme colors

Uses the base CSS custom property defaults — no
overrides needed.
