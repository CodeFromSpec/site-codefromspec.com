# SPEC/html/sections/theory

# Public

## Rendering rules

The navigation paragraph — the last paragraph in
the content, containing only links separated by
` · ` — must be rendered outside of any `<section>`,
as a direct child of the container `<div>`.
