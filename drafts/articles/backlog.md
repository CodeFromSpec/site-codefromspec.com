# Article backlog

Ideas without a draft yet. One line each, with the trigger that
should promote them to a draft.

- **The context firewall** — layers as context compression: an
  artifact handoff makes everything upstream invisible
  downstream; "pipeline depth is free in context." Material:
  framework docs/LAYERS.md (the concept is fully developed there,
  unpublished as article). Trigger: whenever; ready to write.
- **A platform team for the whole company** — the rationale's
  north-star analogy expanded into an operating model. Trigger:
  DO NOT write until a real non-technical contributor ships a
  spec change on a real project; then it writes itself as a
  report ("what happened when the accountant edited the spec").
- **What we have learned so far** — retrospective; the eight AI
  testimonials in drafts/testimonials/ are the dated raw material
  (pain → feature arcs: tag-only updates → manifest; delete-and-
  regenerate → disposition). Trigger: a few months of accumulated
  record, per the human.
- **The CLAUDE.md contamination note** — confinement in practice:
  CLAUDE.md is auto-loaded by subagents and violates confinement;
  AGENTS.md is orchestrator-only (cfs-init-session encodes this).
  Small; could be a section inside a future confinement follow-up
  rather than standalone.
- **The sixth generation** — the GL ladder read correctly: every
  prior generation raised abstraction keeping the translator
  deterministic; this is the first with a stochastic translator,
  and everything (the pair, staleness, verification) follows.
  Core already stated as premise 1 of the Definition page;
  standalone article only if the historical framing earns its
  own space. Low priority.
- **Verified coupling / answering the "bitter lesson"** — direct
  response to Thoughtworks' "handcrafting detailed rules for AI
  doesn't scale": the tree is not rules for the AI, it is the
  system's knowledge with the AI downstream. Trigger: when the
  SDD-responses work (R-C9) is developed for publication.
- **Independence by construction** — possibly redundant: the
  immune-system article + TESTING.md already carry it; standalone
  only if the topic needs its own URL for citation.

Related non-article work parked elsewhere: SDD critique analysis
(drafts/sdd-*.md, with its own handoff), essay revision agenda
(the-llm-is-not-a-compiler — see order.md notes), on-living-in-git
consolidation (absorbs from-artifact-tags).
